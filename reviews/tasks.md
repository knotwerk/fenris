# Consolidated Migration Backlog

This backlog is generated from pair review and existing harness evidence. Items stay open until their associated test and fixture gates pass.

## Scheduler Core

| Task | Priority | Status | Findings | Required proof |
| --- | --- | --- | --- | --- |
| Implement `carbon-scheduler-core` state model with `TaskletId`, `ChannelId`, lifecycle enum, run queue, blocked queues, counters, and deterministic trace events. | P0 | done | `SCH-CORE-001`, `SCH-TASKLET-001`, `SCH-CHANNEL-001` | `cargo test --workspace` and `cargo run -p xtask -- scheduler-fixtures` pass `run_order`, `blocking_send`, `blocking_receive`, `send_receive_match`, `deadlock`; evidence is written to `target/carbon/evidence/scheduler-fixtures.json`. |
| Drain tasklet/channel/scheduler lifecycle ownership out of `carbon-scheduler-python` and behind Rust-owned core IDs plus FFI handles. | P0 | partial | `SCH-CORE-001`, `SCH-FFI-001`, `SCH-FFI-004` | Initial `CoreScheduler` handle tests now cover Rust-owned `CoreTaskletId`/`CoreChannelId`/`CoreRunQueueId` unbuffered channel rendezvous state, live bridge run-queue FIFO/count/remove/pop/clear behavior, per-run-queue current-tasklet identity, scheduled-state authority, public active/all-time tasklet lifetime counters, public active schedule-manager lifetime counters, public active channel lifetime counters, public last-timeout counter storage, public nested-tasklet mode storage, cyclic tasklet parent rejection, public schedule and per-run-queue channel callback registration storage, schedule-callback dispatch metadata, tasklet channel-callback skip storage, tasklet callable-bound storage, tasklet bound-argument storage, tasklet block-trap storage, tasklet highlighted storage, tasklet context storage, tasklet callsite metadata storage, tasklet timing metric storage, tasklet continuation/kill delivery-state storage, transient current-tasklet scheduled snapshots, explicit pause/resume lifecycle transitions, balance signs, blocked queues, single blocked-queue membership, preference clamping, close/open/clear, active/blocked channel counts, unblock-all-active-channel cleanup, remove-blocked cleanup, block-trap no-mutation, operation-result preferred peer-immediate handoff, tasklet parent and main identity, tasklet runtime snapshots, and core-owned kill/pending-exit state with blocked-channel cleanup plus dead-tasklet no-op behavior. Live PyO3 tasklet/channel objects now carry opaque core handles, route unbuffered channel balance/preference/block-trap/blocked-queue transitions through `CoreScheduler` without bridge-local Rust channel blocked sender/receiver queue fields, consume `CoreChannelOperationResult` sender/receiver IDs plus balance and immediate peer handoff for covered unbuffered send/receive transfer decisions, use core snapshots for blocked send/receive tasklet state projection, live channel-continuation scheduled/paused/delivery-state projection, current-tasklet channel handoff requeue projection, `schedule_remove` pause projection, tasklet callable-bound call/setup/insert guards, tasklet bound-argument `dont_raise` guards, tasklet highlighted getter/setter reconciliation, tasklet context getter/setter and C API context reconciliation, tasklet callsite metadata getter/setter reconciliation, tasklet `startTime`/`endTime`/`runTime` getter/setter reconciliation, and the sender-preferred pre-receive scheduling probe, use core queue-front results for `channel.queue` introspection, use core run queues for covered scheduler FIFO/count/remove/pop paths without Python runtime sync writing the core scheduled flag, read public Python/C API scheduler run counts from CoreScheduler runnable counts instead of bridge-local PyObject registry length, read public `getcurrent()`/`tasklet.is_current` and C API `PyScheduler_GetCurrent` from CoreScheduler per-run-queue current-tasklet identity, read public Python/C API active/all-time tasklet counters, active schedule-manager counts, active channel counts, C API last-timeout counts, public nested-tasklet mode, public schedule and per-run-queue channel callback registration state, schedule-callback dispatch metadata, tasklet main identity, tasklet parent identity, tasklet channel-callback skip state, tasklet callable-bound state, tasklet bound-argument state, tasklet block-trap state, tasklet highlighted state, tasklet context state, tasklet callsite metadata state, tasklet timing state, and tasklet continuation/kill delivery state from CoreScheduler counters/state instead of bridge-local atomics, initialized flags, or tasklet-local booleans; PyO3 tasklets no longer store bridge-local Rust blocked, block-trap, callable-bound, args-bound, or counted-active flags, so public blocked state and blocked run/switch guards read `CoreScheduler` `blocked_on` snapshots directly, public block_trap getter/setter and C API PyTasklet_GetBlockTrap/PyTasklet_SetBlockTrap read and write CoreScheduler snapshots directly, and tasklet lifetime retirement is decided by CoreScheduler. Current-thread runnable PyObject registries remain thread-local compatibility storage while CoreScheduler remains ordering authority, covered bind/remove/insert/switch pause paths use core pause/resume, public tasklet identity/state/timing/highlighted/context/callsite metadata getters, public channel queue-front and balance/preference/closing/closed getters with no bridge-local Rust channel blocked sender/receiver queue fields or balance/preference/closing/closed fields, public call/setup/insert/run/switch/bind/unbind/dont_raise invalid-state guards, current-tasklet block-trap channel guards, C API PyTasklet_IsMain/PyTasklet_Alive/times-switched/block-trap/context values, last-timeout counter values, nested-tasklet mode, and schedule/channel callback registration state read core state, public insert avoids stale bridge lifecycle resync when the core already owns scheduled state, and core snapshots drive dead-tasklet kill no-op decisions, timeout completion accounting, and paused direct-run eligibility while all `carbon-scheduler-python` tests pass. Completion still requires `target/carbon/evidence/rust-scheduler-python.json` to record `core_ownership_status.status=complete`; core snapshots become authoritative for remaining Greenlet lifecycle decisions, PyO3 tasklet/channel objects hold Python callable/exception/refcount/payload data only, unchanged Python/C API tests, in-process C API probes, scheduler fixtures, and IO `Scheduler.h` smoke still pass. |
| Add a no-Python native Rust scheduler benchmark lane. | P0 | partial | `SCH-PERF-NATIVE-001` | `bench-scheduler-architecture` now writes `scheduler-architecture.json`, a dedicated target-architecture test that joins same-pressure Rust scheduler bridge rows from `scheduler-comparison.json` to native CoreScheduler runnable-drain and channel-rendezvous rows without Python objects, Greenlets, PyO3, or refcount work in the hot path. `bench-scalability --families native-scheduler` still records the broader native matrix, including larger queue/domain wakeup probes. Rows are marked `no_python_hot_path=true` and `rust_native_no_python_architecture_probe_not_legacy_api_comparable` so they are reported separately from same-Python-API bridge parity rows. Remaining proof is adding real game-shaped native workloads, memory-per-tasklet/channel thresholds, overload/cross-domain stress beyond the local queue model, and CI/report thresholds. |
| Add semantic fixture runner for `fixtures/scheduler/*.json`, including ordered event comparison and final state assertions. | P0 | done | `cargo test --manifest-path carbon-scheduler-rs/Cargo.toml -p carbon-scheduler-trace` validates all current fixture files. |
| Add fixtures for targeted `tasklet.run()` in nested and non-nested modes plus invalid direct run/switch no-mutation. | P0 | done | `SCH-PYTEST-SCHED-002`, `SCH-TASKLET-002` | `tasklet_run_nested_target_first`, `tasklet_run_nested_target_middle`, `tasklet_run_no_nested_target_middle`, and `tasklet_invalid_run_switch_no_mutation` pass under `cargo run -p xtask -- scheduler-fixtures`; `tasklet_run_no_nested_target_middle` also proves the non-nested FRONT_PLUS_ONE targeted-run boundary with trace expectations. |
| Add nested tasklet creation and `scheduler.schedule()` fixtures. | P0 | done | `SCH-PYTEST-SCHED-003`, `SCH-PYTEST-SCHED-004`, `SCH-UTILS-005` | Single-level, multi-level, blocked-yield, and `scheduler_schedule_back_reschedule_order` fixtures pass for nested and non-nested modes under `cargo run -p xtask -- scheduler-fixtures`, match the legacy completion strings from `test_scheduler.py`, and prove `scheduler.schedule()` BACK requeue ordering. |
| Add bounded pumping support for `run_n_tasklets(1)` and timeout counter modeling. | P1 | partial | `SCH-CORE-005` | The Python/C API bridge now passes the real legacy `Scheduler.cpp` run-n and timeout completed/switched counter tests; pure scheduler-core now has event-checked `scheduler_run_n_tasklets_one_fifo_boundary`, four trace-expectation `scheduler_run_n_tasklets_limited_*schedule_order*.json` fixtures, `scheduler_timeout_zero_runs_one_tasklet`, and `scheduler_timeout_large_counts_all_tasklets` plus the final-state `scheduler_timeout_zero_leaves_remaining_scheduled` fixture proving one-tasklet bounded pumping, repeated limited schedule-order pumping for nested and non-nested modes, schedule-resumed tasklets after 5/8 one-tasklet pumps in the nested schedule variants, expired-timeout progress, timeout counter values, and remaining runnable tasklets after an expired timeout. Real monotonic-time policy remains open. |
| Implement tasklet lifecycle operations: setup, insert, remove, pause, kill, direct switch, times-switched counter. | P0 | partial | `SCH-TASKLET-001`, `SCH-PYTEST-TASKLET-001`, `SCH-TASKLET-006` | Core fixtures cover dead, blocked, paused, removed, killed, switched, nested switch/resume counts, blocked switch-chain unwind after channel wakeup, tasklet `times_switched_to` receive/schedule/rebind reset behavior, and nested parent/depth tasklets. The handle API now exposes `CoreTaskletSnapshot`, callable-bound state, bound-argument state, block-trap state, dont_raise state, highlighted state, context state, method/module/file/line/parent-callsite metadata, timing metrics, explicit pause/resume operations, schedule-clears-paused behavior, transient scheduled snapshots, parent and main identity accessors with cyclic parent rejection, `CoreTaskletKillResult` for immediate/pending kill state, and `CoreRunQueueId` scheduler FIFO/count/remove/pop operations; the PyO3 bridge uses core pause/resume for covered bind/remove/insert/switch pause paths, checks core paused snapshots for direct-run eligibility, reads public tasklet identity/parent/lifecycle/scheduling/block-trap/switch-count/timing/dont_raise/highlighted/context/callsite metadata properties from core snapshots, and no longer stores Rust-side tasklet blocked or block-trap mirrors, so public `tasklet.blocked` and blocked run/switch guards read `CoreScheduler` `blocked_on` snapshots directly while public `block_trap` getter/setter and C API block-trap access read/write core snapshots directly. The bridge rejects call/setup/insert when core callable-bound state is false even though the PyO3 tasklet still stores the actual callable object, rejects `dont_raise` changes when core bound-argument state is true without a bridge-local args-bound flag even though PyO3 still stores the actual args/kwargs objects, uses core dont_raise snapshots for exception suppression while PyO3 keeps the optional context-manager and exception-handler callables, reconciles highlighted getter/setter state through core snapshots, reconciles context getter/setter state and `PyTasklet_GetContext` through core snapshots, reconciles method/module/file/line/parent_callsite getters and setter through core snapshots, and reconciles `startTime`/`endTime`/`runTime` through core snapshots with no bridge-local Rust timing metric fields, and reads `times_switched_to` from core snapshots with no bridge-local Rust switch-count field. Remaining work is making snapshots authoritative for every Greenlet lifecycle transition, Python exception delivery path, and broader counter metrics. |
| Implement switch trap and block trap rejection without queue mutation. | P0 | partial | `SCH-CORE-007`, `SCH-CHANNEL-004`, `SCH-PYTEST-CHANNEL-005` | `scheduler_switch_trap_no_state_mutation` now records trace expectations for all eight trapped operations plus zero mutating `scheduler.schedule`/`scheduler.schedule_remove` events, while `scheduler_switch_trap_nested_level`, `channel_block_trap_no_balance_change`, and `channel_block_trap_nonblocking_transfer` assert post-error balances, queues, run counts, and counted switch-trap levels. `channel_raise_exception_reblock_cleanup` now covers the failed-yield cleanup path where a blocked sender/receiver catches an externally raised exception, re-enters the channel operation, and preserves balance/queue order. Broader interrupted transfer cleanup remains under `SCH-CHANNEL-004`. |
| Implement unbuffered channel balance, FIFO blocked queues, preference `-1/0/1`, close/open, and queue introspection. | P0 | partial | `SCH-CHANNEL-001`, `SCH-CHANNEL-002`, `SCH-CHANNEL-003`, `SCH-CHANNEL-007` | Basic balance/blocking, receiver-preferred transfer, sender preference, simple neutral preference, multi-party neutral ordering, main-side preference order, close/open, blocked queue order, and queue introspection fixtures now pass. The `CoreScheduler` handle API covers Rust-owned balance signs, queue fronts, close/open/clear, remove-blocked cleanup, block-trap no-mutation, and immediate peer-handoff preference with `CoreTaskletId`/`CoreChannelId`; the PyO3 bridge now allocates those handles for live tasklet/channel objects, routes balance/preference/block-trap/blocked-queue transitions through the core, uses core send/receive results to select matched Python tasklets by core ID plus decide immediate peer handoff instead of using Python queue-front or local preference checks, uses `CoreScheduler::queue_front` for `channel.queue` object lookup with no bridge-local Rust channel blocked sender/receiver queue fields, and reads public channel balance/preference/closing/closed getters from core snapshots with no bridge-local Rust channel balance/preference/closing/closed fields. Python-visible payload registry contents, blocked-tasklet lifetime refs, broader PyObject identity adapters, and exact error details remain open. |
| Model channel exception payloads symbolically before Python exception FFI wiring. | P1 | partial | `SCH-CHANNEL-005`, `SCH-TASKLET-005`, `SCH-PYTEST-TASKLET-002` | Core trace can represent tasklet-to-tasklet and main-receive `send_exception`/`send_throw` transfer outcomes with ordered event fixtures. It also now covers symbolic immediate `tasklet.throw`, pending `tasklet.throw`, and catchable pending `TaskletExit` delivery in `tasklet_throw_pending_immediate_delivery`. Exact Python exception object/value/traceback behavior remains in the bridge/FFI layer. |
| Keep `QueueChannel` as the legacy Python wrapper over the Rust-backed base `channel` unless later parity proves a Rust buffered channel is required. | P1 | done | `SCH-QUEUECHANNEL-001`, `SCH-QUEUECHANNEL-002`, `SCH-QUEUECHANNEL-007` | The PyO3 package smoke executes the real `scheduler/__init__.py`, proves `QueueChannel` subclasses the Rust-backed `channel`, sets `preference=1`, buffers send/receive values, reports length/balance from `data_queue`, raises queued exception/throw payloads, directly wakes blocked receivers, and all 10 unchanged legacy `test_queuechannel.py` tests pass in `cargo run -p xtask -- rust-scheduler-python`. |
| Add QueueChannel hybrid receive and main-drain fixtures if buffered semantics move into Rust. | P2 | deferred | `SCH-QUEUECHANNEL-003`, `SCH-QUEUECHANNEL-005`, `SCH-QUEUECHANNEL-006`, `SCH-QUEUECHANNEL-008` | Not needed for the current compatibility path because QueueChannel remains the legacy Python wrapper; add pure-core buffered-channel fixtures only if QueueChannel is deliberately migrated into Rust core later. |
| Add fixture-runner teardown and invariant checks matching `test_utils.py`. | P0 | partial | `SCH-UTILS-001` | `carbon-scheduler-trace` now validates every scheduler fixture for event sequence order, event-level cached/calculated run count equality, runnable snapshot length consistency, active/all-time tasklet counts, active channel count, blocked tasklet state, blocked tasklet/channel counts, single blocked-queue membership, channel balance, queue-front consistency, and channel/tasklet blocked-queue cross-links. The fixture runner also supports an opt-in teardown script; `fixture_teardown_blocked_channel_cleanup` proves named blocked sender/receiver cleanup returns the core scheduler to `run_count=1`, `blocked_tasklet_count=0`, `blocked_channel_count=0`, and `active_tasklet_count=1`, while `fixture_teardown_unblock_all_channels` proves scheduler-level `unblock_all_channels` counts and clears only nonzero-balance active channels. `channel_blocked_membership_single_queue_cleanup` proves blocked sender/receiver membership across two channels plus kill/clear cleanup and repeated no-op cleanup. The PyO3 bridge now separately covers schedule-manager refcount/weakref/thread-cache teardown and direct CoreScheduler-backed active channel counter return-to-zero; broader thread/GC cleanup remains open in the FFI/core-registry layer. |

## Scheduler FFI And Python Boundary

| Task | Priority | Status | Findings | Required proof |
| --- | --- | --- | --- | --- |
| Add reproducible legacy scheduler baseline evidence probe. | P0 | done | `REPORT-001`, `SCH-UTILS-006` | `cargo run -p xtask -- legacy-scheduler native-linux` writes `target/carbon/evidence/legacy-scheduler.json`, records host CMake/CTest/Python metadata, and now passes the unchanged legacy Python unittest suite plus C API CTest cases with `report_ready=true`. |
| Keep supported-platform legacy scheduler import as a fallback. | P1 | done | `REPORT-001`, `SCH-UTILS-006`, `SCH-FFI-001` | Native Linux legacy scheduler baseline evidence is now available on this host, so supported-platform import is no longer a current report blocker. The import path remains available for Windows/macOS artifacts when needed for cross-host confirmation. |
| Define Rust C ABI crate with opaque handles, `carbon_scheduler_abi_version()`, error/status enum, and panic containment. | P0 | partial | `SCH-FFI-001` | Initial crate exposes ABI version, status enum, and panic containment tests; opaque handles and invalid-handle C ABI tests remain open. |
| Add initial PyO3 build-flavor module/package smoke surface and evidence gate for the Rust scheduler Python boundary. | P0 | done | `SCH-UTILS-006`, `SCH-PY-BRIDGE-001`, `SCH-QUEUECHANNEL-007` | `cargo run -p xtask -- rust-scheduler-python` runs `cargo test --manifest-path carbon-scheduler-rs/Cargo.toml -p carbon-scheduler-python`, builds the Rust cdylib, imports `_scheduler`, `_scheduler_debug`, `_scheduler_trinitydev`, and `_scheduler_internal` from `target/carbon/python`, imports the legacy `scheduler` package through `PYTHONPATH`, runs all 210 unchanged legacy scheduler Python unittest cases against the Rust extension, compiles/runs an expanded C++ `Scheduler.h` tasklet lifecycle/run-control/channel-preference/invalid-argument/inside-tasklet-send smoke against the Rust `_C_API` capsule, compiles/runs generated source-slice binaries for the real legacy `capiTest/Tasklet.cpp`, `Channel.cpp`, and `Scheduler.cpp` tests one test per child process, and writes `target/carbon/evidence/rust-scheduler-python.json` with `report_ready=false`. |
| Preserve Python capsule `_C_API` layout or ship a compatibility adapter matching `Scheduler.h`. | P0 | partial | `SCH-FFI-001`, `SCH-PYTEST-SCHED-005` | A named `scheduler._C_API` PyCapsule with non-null initial ABI-table pointer, type/exception slots, callable `PyTasklet_New`, `PyChannel_New`, `PyScheduler_GetCurrent`, and `PyScheduler_GetScheduler` entries, callable tasklet/channel type/property entries, callable read-only scheduler counter entries including CoreScheduler-backed run-count, active tasklet count, all-time tasklet count, and active channel count, simple `PyTasklet_Setup` plus `PyScheduler_RunNTasklets` execution, safe C API tasklet/channel/scheduler smoke paths, C API invalid tasklet/channel/null argument rejection, C API channel preference get/set/clamping, C API `PyChannel_Send` from inside a running tasklet with blocked sender balance/queue accounting, full unchanged legacy scheduler Python unittest coverage, callback registration/invocation smoke, active Python-level Greenlet/channel exception/send_throw/thread-cleanup/switch/raise/kill smoke paths, tasklet kill/invalid construction/weakref/cyclic cleanup/TaskletExit smoke, switch-trap raised-callable smoke, channel block/deadlock/preference/refcount/inter-thread/cleanup/closed-state behavior, invalid channel construction, channel iterator smoke, block-trap balance no-mutation checks, QueueChannel buffered wrapper behavior, an expanded C++ `Scheduler.h` tasklet lifecycle/run-control/channel-preference smoke, real `capiTest/Tasklet.cpp`, `Channel.cpp`, and `Scheduler.cpp` source-slice runs covering 36 C API tests, and installed release-wheel `_scheduler`/`scheduler` smoke are validated. An optional in-process source-slice probe is now recorded and currently fails after the first test in each slice: the next embedded `scheduler` import sees `_scheduler.channel` as `None`, so `QueueChannel(_scheduler.channel)` raises `TypeError`. Full in-process legacy `SchedulerCapiTest`/`capiTest` binary coverage and broader ABI failure tests remain open. |
| Implement Python bridge for tasklet/channel/scheduler types with GIL/refcount/GC/weakref safety while keeping it a compatibility wrapper, not the scheduler owner. | P0 | partial | `SCH-FFI-002`, `SCH-FFI-003`, `SCH-FFI-004`, `SCH-PYTEST-TASKLET-004` | PyO3 module population, normal CPython import of `_scheduler.so`, public symbol smoke, named `_C_API` capsule shell with callable constructors/current/scheduler getters, tasklet/channel type/property entries, read-only scheduler counters, simple tasklet setup/run execution, Python-level `scheduler.tasklet(callable)(args)` plus `scheduler.run_n_tasklets(1)`, direct `tasklet.run()` for simple queued callables and core-paused bind/remove tasklets, all 210 unchanged legacy scheduler Python unittest cases, safe C API tasklet/channel/scheduler smoke paths, an expanded C++ `Scheduler.h` tasklet lifecycle/run-control/channel-preference/invalid-argument/inside-tasklet-send smoke, real `capiTest/Tasklet.cpp`, `Channel.cpp`, and `Scheduler.cpp` source-slice runs covering 36 C API tests, installed release-wheel smoke, callback registration/invocation smoke, tasklet kill/invalid construction/weakref/cyclic cleanup/TaskletExit smoke, switch-trap raised-callable smoke, channel blocking/deadlock/preference/refcount/inter-thread/cleanup accounting, block-trap send/receive errors, closed/open channel basics, invalid channel construction, channel iterator smoke, block-trap balance no-mutation checks, `channel` subclass constructor/init shape, QueueChannel buffered wrapper path including queued exception/throw paths, Greenlet continuation/yield resume, preference setter, stable current tasklet for `block_trap`, nested-tasklet mode, schedule and channel callback registration state, thread cleanup, ABI version exposure, live CoreScheduler handle allocation/mirroring for PyO3 tasklet/channel unbuffered channel state, core-owned live run-queue FIFO/count/remove/pop behavior, per-run-queue current-tasklet identity, scheduled-state authority, public Python/C API scheduler getcurrent and run-count authority, public Python/C API active/all-time tasklet lifetime counter authority with no bridge-local counted-active flag, public Python/C API active schedule-manager counter authority, public Python/C API active channel counter authority, public C API last-timeout counter authority, core-ID based matched sender/receiver selection plus core-owned payload handoff tokens and immediate peer handoff, blocked send/receive tasklet state projection, live channel-continuation scheduled/paused/delivery-state projection, current-tasklet channel handoff requeue projection, and `schedule_remove` pause projection in covered unbuffered/scheduler-yield paths, live tasklet current/main/parent identity, runtime, timing, block-trap, dont_raise, highlighted, context, callsite metadata, callable-bound, bound-argument, delivery-state, and channel-callback skip snapshots, public tasklet identity/state/timing/dont_raise/highlighted/context/callsite metadata getters, no bridge-local Rust tasklet blocked, block-trap, callable-bound, args-bound, counted-active, timing metric, or switch-count fields, no Channel-owned payload map, public channel state getters, public tasklet call/setup/insert/run/switch/bind/unbind/dont_raise invalid-state guards, current-tasklet block-trap channel guards, C API PyScheduler_GetCurrent and PyTasklet_IsMain/PyTasklet_Alive/times-switched/block-trap/context getter authority, C API PyTasklet_GetBlockTrap/PyTasklet_SetBlockTrap core snapshot authority, public C API last-timeout counter authority, public schedule and per-run-queue channel callback registration plus schedule-callback dispatch metadata authority, explicit core pause/resume lifecycle transitions for covered bind/remove/insert/switch pause paths, and core-backed blocked state, block_trap getter/setter, dead-tasklet kill no-op, timeout completion, paused direct-run, call/setup/insert callable-bound, dont_raise getter/setter and exception-suppression behavior, highlighted getter/setter reconciliation, context getter/setter and C API context reconciliation, callsite metadata getter/setter reconciliation, `startTime`/`endTime`/`runTime` getter/setter reconciliation, and `dont_raise` bound-argument decisions are covered; `rust-scheduler-python.json` records `core_ownership_status=partial` because Python payload/exception objects still live in a `CorePayloadToken` bridge registry, actual callable/args/kwargs object storage/execution, context-manager/exception-handler callable objects, Greenlet tasklet lifecycle authority, callback callable/function-pointer storage and actual invocation, refcount/GC, and exact PyObject identity adaptation still remain bridge-owned. Full in-process legacy C API binary coverage, broader wheel flavor/dependency/install matrices, broader failure-mode hardening, and final core-ownership drain remain open. |
| Preserve build-flavor extension import names and scheduler package re-export behavior. | P0 | partial | `SCH-UTILS-006` | Release/debug/trinitydev/internal module imports and `sys.modules['_scheduler']` aliasing work from `target/carbon/python`; the release-flavor unchanged legacy unittest suite runs from that package path; installed release-wheel `_scheduler` and `scheduler` imports work from site-packages; broader flavor-matrix wheel execution remains open. |
| Preserve schedule-manager Python refcount and CoreScheduler-backed active-manager teardown invariants. | P0 | done | `SCH-UTILS-002`, `SCH-FFI-004` | `schedule_manager_wrapper_lifecycle_matches_legacy_thread_cache` proves `scheduler.get_schedule_manager()` returns the cached thread manager, C API `PyScheduler_GetScheduler` returns the same object, direct wrapper weakrefs clear, cached manager weakrefs stay live, `sys.getrefcount(scheduler.get_schedule_manager()) == 2`, and active manager count reads CoreScheduler and returns to its pre-thread value after teardown. |
| Translate core status and channel/tasklet errors to legacy Python exceptions and messages where tests require exact behavior. | P1 | open | `SCH-FFI-005` | Python tests for RuntimeError/ValueError/TaskletExit and send_throw traceback behavior pass. |
| Add callback invocation adapter for schedule and channel callbacks after trace callback points exist. | P1 | partial | `SCH-CORE-006`, `SCH-CHANNEL-008`, `SCH-FFI-006` | Core trace fixtures now cover scheduler callback previous/next switch points and channel callback will-block metadata. Python-level callback registration get/set storage is covered by unchanged tests, and schedule/channel callback registration, schedule-callback dispatch identity/enable metadata, plus tasklet channel-callback skip state are CoreScheduler-backed. Remaining bridge work is callback callable/fast-C invocation hardening, exception/reentrancy behavior, and callback lifetime/refcount cleanup. |

## Realistic IO Workloads

| Task | Priority | Status | Findings | Required proof |
| --- | --- | --- | --- | --- |
| Extract socket accept/connect/send/receive scheduler workloads from `carbonengine/io`. | P1 | partial | `SCH-IO-001` | `cargo run -p xtask -- io-workloads` now records local TCP loopback accept/connect/send/receive request-cycle latency, throughput, CPU, and RSS for a Python baseline and Rust scheduler bridge client tasklet with five process samples per implementation by default and 400 aggregated request latency samples. It also validates `fixtures/io/socketpair_recv_send_wake.json` as a timing-free normalized event-order fixture for recv block, send, IO callback wake, receiver resume, and balance recovery. Normalized legacy `carbonio`/`_socket` extension semantic trace capture/comparison remains blocked on supported legacy artifacts. |
| Extract SSL read/write wakeup workloads and exception propagation from `_ssl.c` tests. | P1 | partial | `SCH-IO-002` | `cargo run -p xtask -- io-workloads` now records local TLS loopback handshake/read/write request-cycle latency, throughput, CPU, and RSS using the `carbonengine/io` certificate fixtures with five process samples per implementation by default and 120 aggregated request latency samples. It validates `fixtures/io/ssl_handshake_read_write_wake.json` and `fixtures/io/ssl_send_throw_error_wake.json` as timing-free normalized event-order fixtures, and the IO-facing `Scheduler.h` smoke proves `PyChannel_SendThrow` propagates a `RuntimeError` through a blocked receiver. Real legacy `_ssl` extension wakeup/error propagation traces remain open. |
| Preserve `c_channel` scheduler C API usage, especially balance-based wake decisions and `PyChannel_SendThrow`. | P0 | partial | `SCH-IO-003` | `cargo run -p xtask -- io-workloads` compiles and runs a real `Scheduler.h` C++ consumer against the Rust scheduler capsule, proving `PyChannel_GetBalance` blocked-receiver accounting, C API send wakeup, and `PyChannel_SendThrow` error propagation. The same gate now validates fixture-only `fixtures/io` semantic trace shapes, but broader `carbonengine/io/src/c_channel.*` and `carbonio` extension corpus parity remains blocked on supported legacy Carbon IO artifacts. |

## Resources

| Task | Priority | Status | Findings | Required proof |
| --- | --- | --- | --- | --- |
| Implement Rust resource model and YAML/CSV legacy adapters. | P0 | partial | `RES-CORE-001` | Initial imports parse legacy YAML/CSV ResourceGroup fixtures, including v0 prefix paths; Linux/macOS/Windows create-group YAML, skip-compression YAML, CSV, and prefixed CSV goldens round-trip byte-for-byte with platform-specific `BinaryOperation` values preserved; the large `Indicies` normal and binary-operation v0 CSV catalogs export byte-for-byte to their v0.1 YAML goldens, and the matching YAML corpus round-trips byte-for-byte; malformed YAML/CSV import result categories, future-minor version clamping, and future-major version rejection match legacy expectations; exports, Linux create-group YAML/CSV/prefix/skip-compression outputs, YAML/CSV merge outputs, CSV diff outputs, YAML remove-resource outputs, and matching Rust CLI process artifacts for those catalog operations match goldens byte-for-byte; broader corpus coverage and cross-host create-group generation remain open. |
| Implement bundle pipeline with chunking and byte compatibility checks. | P0 | partial | `RES-BUNDLE-001` | BundleGroup manifest import/export now matches create-bundle local CDN, create-bundle remote CDN, and unpack fixture goldens byte-for-byte; ResourceTools stream splitting now covers many files into many uncompressed chunks and many files into one compressed chunk with exact reconstruction; local bundle create emits manifest/chunk bytes; remote-CDN bundle create emits compressed manifest/chunk payload bytes with legacy gzip headers; create-bundle zero-chunk and missing-resource-source failure cleanup is covered; local unpack restores resource bytes; compressed remote-CDN unpack restores resource bytes for the current fixture corpus; local unpack CLI evidence validates a 42-chunk boundary fixture with 41 full chunks, one tail chunk, manifest payload checksums, and resources spanning chunk boundaries; missing-chunk unpack failure cleanup, remote-requested-local chunk failure cleanup, and local-requested-remote compressed chunk failure cleanup are covered; local remote-CDN mirror/cache tests prove cache hit, bad-cache replacement, and checksum-failure behavior; the remote-CDN unpack CLI now records first-run downloads and second-run cache hits; and the create/unpack bundle CLI process cases emit matching payloads plus stable legacy YAML manifests. Broader bundle corpus, network-backed remote/catalog behavior, and failure behavior remain open. |
| Implement patch generation/application compatibility. | P0 | partial | `RES-PATCH-001` | PatchGroup manifest import/export now matches create-patch, chunked create-patch, and old patch fixture goldens byte-for-byte; local CDN patch payload files are validated by manifest location, uncompressed size, and checksum; low-level BSDIFF corruption rejects short payloads, invalid headers, truncated streams, and target-length mismatches; Rust-generated BSDIFF payloads match normal, chunked, and old-layout local CDN payload bytes; normal and chunked local create-patch CLI manifest/payload output matches legacy goldens; no-change local create-patch CLI output matches the expected empty manifest/payload semantics; zero-chunk and missing previous/next resource-source create-patch failure cleanup is covered; a synthetic copy-only create/apply CLI case emits only copy records with no binary patch payloads and reconstructs the latest resource; normal, chunked, and old-layout local patch apply fixtures reconstruct expected resource bytes, including old-layout no-removal semantics; malformed local apply manifests reject zero apply chunk size, target offset overflow, source range overflow, and overlapping copy ranges; apply-patch CLI process cases reconstruct the normal, chunked, and old-layout payload directories; and missing previous/next resource input plus missing/corrupt patch payload apply failures leave no output tree. Broader temp-file cleanup modes and larger corpus parity remain open. |
| Implement resource tools parity for MD5, FNV, rolling checksum, gzip, file streams, matching chunks, and case-sensitive path behavior. | P0 | partial | `RES-TOOLS-001`, `RES-TOOLS-002` | Initial `carbon-resources-core` tests cover MD5 string/file checksums, MD5 stream checksums over legacy file chunks, legacy FNV, gzip fixture decompression/roundtrip with exact legacy header normalization for generated gzip members, chunked gzip stream compression/decompression, FileDataStreamIn chunked reads, FileDataStreamOut byte output, CompressedFileDataStreamOut gzip roundtrip, rolling checksum formula, FindMatchingChunks string cases, FindMatchingChunk file offset cases, CountMatchingChunks patch fixture offsets, ChunkIndex generation/persisted lookup, ChunkIndex checksum-filtered generation/lookup, and case-sensitive exact filter paths; broader path behavior, streaming edge cases, and larger file/compression corpora remain open. |
| Implement filter engine with globset/bitmap or equivalent deterministic matching. | P1 | partial | `RES-FILTER-001` | Initial parser/matcher tests cover prefix maps, `*`, `...`, include/exclude rules, local respath rule quirks, multi-prefix cases, a generated wildcard/ellipsis path matrix, and generated include/exclude section-property cases for section recovery, exact-file overrides, direct wildcard rules, and recursive ellipsis rules; filter index mapping parsing and CreateFromFilter fixture output match legacy goldens byte-for-byte; broader filter-file corpus and fuzz cases remain open. |
| Add Rust resources CLI parity gate for legacy `ResourcesCliTest` behavior. | P0 | partial | `RES-CLI-001` | `cargo run -p xtask -- rust-resources` now runs 70 process-level Rust CLI parity cases mapped to `ResourcesCliTest`/`ResourcesLibraryTest`: no-command/invalid-command/`--help` top-level legacy help-shape cases, operation-specific `--help` and no-argument usage-shape cases for all resource commands, dispatcher exit-code cases, create-group YAML/CSV/prefix/skip-compression, create-group-from-filter, local export-resources for directory and filter creation, local and remote-CDN create-bundle manifest/payload output, a local create-and-unpack bundle roundtrip with stable YAML and payload byte checks, create-bundle zero-chunk and missing-resource-source failure cleanup, local and remote-CDN unpack-bundle payload output including 42-chunk boundary evidence, missing-chunk failure cleanup, remote-requested-local chunk failure cleanup, local-requested-remote compressed chunk failure cleanup, and remote-CDN mirror download/cache-hit stats, normal/no-change/chunked/copy-only local create-patch and apply-patch output, create-patch zero-chunk and missing previous/next resource-source failure cleanup, normal, chunked, and old-layout apply-patch payload output, apply-patch missing previous/next resource input plus missing/corrupt patch payload failure cleanup with no output tree, merge YAML/CSV, diff additions/changes/subtractions, remove-resources, ignore-missing success, invalid resource-list failure, and unknown-resource failure semantics. Broader detailed CLI output/error text compatibility, broader apply/unpack corpus coverage, and broader failure behavior remain open. |
| Replace remote probe/download behavior with async object-store adapter only after local parity is green. | P2 | open | `RES-REMOTE-001` | Local object-store simulation passes retry/cancel/error tests. |
| Define Rayon/Tokio runtime split for CPU stages and remote IO stages. | P2 | open | `RES-PIPE-001` | Benchmarks show CPU scaling without changing output bytes. |

## Benchmarks And Report

| Task | Priority | Status | Findings | Required proof |
| --- | --- | --- | --- | --- |
| Add benchmark JSON schema and local Tier 1 runners for scheduler and resources. | P0 | partial | `BENCH-001` | `cargo run -p xtask -- bench` writes Rust-only scheduler/resource samples plus preliminary parity-checked process-to-process create-group, create-group-from-filter, merge-group, diff-group, remove-resources, create-bundle, create-patch, unpack-bundle, and apply-patch legacy/Rust rows with host/toolchain metadata, throughput, wall time, p50/p95 latency, effective CPU burn, CPU percent, peak RSS, and 100k-unit linear estimate stats; `cargo run -p xtask -- io-workloads` adds sampled TCP/TLS loopback baseline-vs-scheduler-bridge workload stats. Allocation counters, production-size validation, legacy Carbon IO comparison, and broader comparable workloads remain open. |
| Mark benchmark results `not comparable` unless parity passes for the same workload. | P0 | partial | `BENCH-002`, `REPORT-001` | Initial benchmark evidence keeps broad speedup suppressed: most rows are Rust-only, while create-group, create-group-from-filter, merge-group, diff-group, remove-resources, create-bundle, create-patch, unpack-bundle, and apply-patch are marked `comparable_process_to_process` with narrow preliminary speed/resource claims. |
| Add release/native comparability coverage for every benchmark lane. | P0 | partial | `BENCH-003` | `scripts/carbon-native-bench.sh bench` records `release-native`, fat LTO, and `target-cpu=native` for the current narrow Rust benchmark rows; `scripts/carbon-native-bench.sh io-workloads` records release-native native CPU context for the TCP/TLS loopback rows. Legacy optimized/release profile metadata, legacy Carbon IO comparability, and broader comparable rows remain open. |
| Investigate resource optimization hot spots after parity coverage lands. | P1 | open | `BENCH-004`, `RES-PIPE-001` | Rayon file/hash/compress stages, compiled filter matching, indexed remove, lower-allocation merge/diff, and streaming hash/compress paths each need byte-for-byte parity plus throughput/CPU/RSS evidence before being claimed. |
| Investigate scheduler optimization hot spots after Python/C API semantics land. | P1 | open | `BENCH-005`, `SCH-CORE-001` | Interned IDs/generational arenas, slotmap-style state, lower-cost queue removals, and typed trace events need fixture parity, invalid-handle tests, and switch/channel/trace overhead benchmarks before being claimed. |
| Generate final HTML report only after parity and benchmark gates exist. | P0 | blocked | `REPORT-001` | Every feature/performance/architecture claim links to passing evidence. |
