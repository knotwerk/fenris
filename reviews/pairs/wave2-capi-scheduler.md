# Wave 2 Pair: capiTest/Scheduler.cpp -> FFI tests

Status: queued.

Legacy source:

- `carbonengine/scheduler/tests/capiTest/Scheduler.cpp`

Rust target:

- `carbon-scheduler-ffi`

## Review Scope

Scheduler control calls, run counts, current tasklet, run limits, timeouts, callbacks, active counters, and ABI return conventions.

