# Wave 2 Pair: capiTest/Tasklet.cpp -> FFI tests

Status: queued.

Legacy source:

- `carbonengine/scheduler/tests/capiTest/Tasklet.cpp`

Rust target:

- `carbon-scheduler-ffi`

## Review Scope

Tasklet construction, setup, insert, block trap, main flag, alive state, kill behavior, switch counts, context, invalid handles, and Python refcount compatibility.

