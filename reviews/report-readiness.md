# Report Readiness

Status: blocked.

The final HTML report is not allowed yet. The repository now has a Rust workspace, a green scheduler fixture gate with linked local Python/C API and IO bridge evidence, an initial `CoreScheduler` owner-thread handle API for Rust-owned unbuffered channel tasklet/channel state and tasklet runtime snapshots, live PyO3 tasklet/channel objects carrying opaque core handles with mirrored unbuffered channel balance/preference/block-trap/blocked-queue transitions, core-ID based matched sender/receiver selection plus core-owned immediate peer handoff for covered unbuffered send/receive paths, core snapshot driven sender-preferred pre-receive scheduling probes, core-selected `channel.queue` fronts, core-owned live run queues and scheduled-state authority, explicit core pause/resume transitions for covered bind/remove/insert/switch pause paths, and mirrored tasklet alive/scheduled/paused/times_switched_to snapshots, a reproducible legacy scheduler CMake/vcpkg baseline probe, an initial Rust scheduler PyO3 cdylib build-flavor import/package/QueueChannel C API constructor/property/counter/simple setup-run smoke gate, all 210 unchanged legacy scheduler Python unittest cases passing against the Rust extension, an expanded C++ `Scheduler.h` tasklet lifecycle/run-control/channel-preference/invalid-argument/inside-tasklet-send smoke passing against the Rust `_C_API` capsule, generated source-slice binaries compiling/running the real legacy `capiTest/Tasklet.cpp`, `Channel.cpp`, and `Scheduler.cpp` C API tests one test per child process, an optional in-process source-slice probe that currently exposes the repeated-interpreter failure after the first test in each slice, an IO-facing C++ `Scheduler.h` channel wake/send_throw semantic smoke passing against the Rust `_C_API` capsule, a fixture-only `fixtures/io` normalized semantic trace corpus for socket recv/send wake, SSL read/write wake, and SSL send_throw error wake, fresh legacy resources evidence, a first Rust resources tools/catalog/filter/create-group/create-from-filter/merge/diff/remove/malformed-imports/local-and-remote-CDN-bundle-create-unpack-bytes/remote-cache-stats/create-bundle-failure-cleanup/missing-chunk-failure-cleanup/patch-manifest-payload-generation-and-local-apply-bytes/create-patch-failure-cleanup slice, sampled loopback TCP/TLS IO workload evidence, and local benchmark samples including preliminary parity-checked process-to-process create-group, create-group-from-filter, merge-group, diff-group, remove-resources, create-bundle, create-patch, unpack-bundle, and apply-patch workloads with wall time, p50/p95 latency, effective CPU burn, CPU percent, peak RSS, throughput, and conservative 100k-unit linear estimates. Full in-process legacy scheduler C API binary parity, final PyO3 ownership drain for Python payload handoff/queue identity adapters/Greenlet lifecycle authority/broader scheduling decisions, full resources Rust parity, captured legacy Carbon IO semantic trace comparison, and broader final benchmark data are still missing.

## Current Evidence Gates

| Gate | Status | Evidence |
| --- | --- | --- |
| Legacy `resources` CTest | green | `cargo run -p xtask -- legacy-resources` runs CTest and writes `target/carbon/evidence/legacy-resources.json` with `121/121` passing. |
| Legacy `scheduler` Python/C API tests | blocked/probed | `CARBON_LEGACY_SCHEDULER_BOOTSTRAP=1 cargo run -p xtask -- legacy-scheduler` writes `target/carbon/evidence/legacy-scheduler.json` with `status=fail`, `report_ready=false`; vcpkg bootstrap now succeeds, but the `carbon-core` vcpkg port declares support only for Windows x64 and macOS. The forced `x64-linux` configure fails before the legacy scheduler baseline can build, first on the `CCPDefines.cpp`/`CcpDefines.cpp` case-sensitive source mismatch and then with broader non-Windows/non-Apple platform guards still expected. `cargo run -p xtask -- legacy-scheduler import <artifact.json|ctest.log> --host-os <windows|macos> --host-arch <x86_64|aarch64>` now imports a non-empty passing supported-platform CTest baseline into the same evidence schema. |
| Scheduler semantic fixture corpus | partial | Forty-five JSON fixtures exist under `fixtures/scheduler/`: 44 assert ordered semantic events and one final-state timeout snapshot asserts expired-timeout counters plus remaining scheduled tasklets; many behaviors still lack fixtures. |
| Rust scheduler semantic fixture gate | green | `cargo run -p xtask -- scheduler-fixtures` passes 45/45 current invariant-checked fixtures and writes `target/carbon/evidence/scheduler-fixtures.json`; each fixture report records structural invariant checks for event sequence order, run-count/tasklet-count consistency, blocked tasklet state, channel balance, queue-front consistency, and channel/tasklet blocked-queue cross-links. That evidence also records `local_python_capi_bridge_link_status=pass` and `local_io_scheduler_bridge_link_status=pass` when the local Rust Python/C API and IO bridge gates are present. |
| Rust scheduler Python boundary | partial green | `cargo run -p xtask -- rust-scheduler-python` runs the PyO3 `_scheduler` cdylib build-flavor import/package/QueueChannel C API constructor/property/counter/simple setup-run smoke tests, all 210 unchanged legacy Python unittest cases including all 10 `test_queuechannel.py` cases, an expanded C++ `Scheduler.h` tasklet lifecycle/run-control/channel-preference/invalid-argument/inside-tasklet-send smoke against the Rust `_C_API` capsule, generated source-slice binaries that compile/run the real legacy `capiTest/Tasklet.cpp`, `Channel.cpp`, and `Scheduler.cpp` C API tests one test per child process, an optional `CARBON_CAPI_GTEST_IN_PROCESS=1` source-slice probe, and a `maturin` release-wheel build/install smoke from a fresh virtualenv, then writes `target/carbon/evidence/rust-scheduler-python.json` with `report_ready=false` and `core_ownership_status.status=partial`. The core has Rust-owned `CoreScheduler` tasklet/channel/run-queue handles for unbuffered channel state, live scheduler FIFO/count/remove/pop behavior, scheduled-state authority, explicit pause/resume lifecycle transitions, and tasklet runtime snapshots, and live PyO3 tasklet/channel objects now carry those handles while mirroring balance/preference/block-trap/blocked-queue transitions, using core run queues for covered scheduler queue paths without Python runtime sync writing the core scheduled flag, using core pause/resume for covered bind/remove/insert/switch pause paths, checking core paused snapshots for direct-run paused eligibility, using core send/receive results to select matched Python tasklets by core ID, deciding immediate peer handoff in covered unbuffered paths, driving the sender-preferred pre-receive scheduling probe from core snapshots, and selecting `channel.queue` fronts from core queue-front IDs. The in-process probe is currently `not_green`: the first test in each slice passes, then the next embedded `scheduler` import fails because `_scheduler.channel` is `None`. Full in-process legacy `SchedulerCapiTest`/`capiTest` binary coverage, broader wheel flavor/dependency/install matrices, broader failure-mode hardening, final PyO3 ownership drain for Python payload handoff/queue identity adapters/Greenlet lifecycle authority, and legacy Carbon IO semantic traces are still open. |
| IO loopback workloads | partial green | `cargo run -p xtask -- io-workloads` runs TCP and TLS loopback request cycles as a Python stdlib baseline and through the Rust scheduler Python bridge, records five process samples per implementation by default, aggregates 400 TCP and 120 TLS request latency samples in `target/carbon/evidence/io-workloads.json`, validates the timing-free `fixtures/io` normalized semantic trace corpus for socket recv/send wake, SSL read/write wake, and SSL send_throw error wake, compiles/runs an IO-facing `Scheduler.h` C++ smoke proving `PyChannel_GetBalance` blocked-receiver accounting, C API send wakeup, and `PyChannel_SendThrow` propagation, and records a structured `legacy_carbonio_semantic_traces` blocker. It keeps `report_ready=false` because the normalized legacy `carbonio`/`_socket`/`_ssl` trace comparison needs a supported Windows/macOS legacy carbonio+legacy scheduler run or supplied prebuilt legacy artifacts. |
| Wave 1 scheduler review | complete | Review queue and pair notes are consolidated for core scheduler, tasklet, channel, Python scheduler/channel/tasklet tests, QueueChannel, and test harness invariants. |
| Rust scheduler tests | initial green | `cargo test --workspace` passes the current Rust scheduler core, trace, FFI bootstrap, and PyO3 smoke tests. |
| Rust resources tests | partial green | `cargo run -p xtask -- rust-resources` writes `target/carbon/evidence/rust-resources.json` for MD5, FNV, gzip with legacy header normalization, rolling checksum, ResourceTools stream/matching/chunk-index coverage for FileDataStreamIn chunked reads, FileDataStreamOut byte output, CompressedFileDataStreamOut gzip roundtrip, MD5 stream checksums, chunked gzip stream compression/decompression, FindMatchingChunks string cases, FindMatchingChunk file offset cases, CountMatchingChunks patch fixture offsets, ChunkIndex generation/persisted lookup, ChunkIndex checksum-filtered generation/lookup, ResourceTools bundle stream splitting for many files into many uncompressed chunks and one compressed chunk with exact reconstruction, legacy YAML/CSV import/export including Linux/macOS/Windows create-group YAML, skip-compression YAML, CSV, and prefixed CSV golden round-trips with platform-specific `BinaryOperation` values, large `Indicies` normal and binary-operation v0 CSV to v0.1 YAML conversion plus YAML corpus round-trips, malformed import result mapping, BundleGroup and PatchGroup manifest import/export, local BundleGroup create/unpack byte checks, remote-CDN compressed BundleGroup create byte checks, a local create-and-unpack bundle roundtrip with stable YAML and payload byte checks, create-bundle zero-chunk and missing-resource-source failure cleanup, compressed remote-CDN BundleGroup unpack byte checks, 42-chunk local unpack boundary evidence, missing-chunk unpack failure cleanup, remote-requested-local chunk failure cleanup, local-requested-remote compressed chunk failure cleanup, local remote-CDN mirror/cache hit, bad-cache replacement, and checksum-failure checks, PatchGroup local CDN payload read/apply/generation byte checks including old-layout apply with no-removal semantics, corrupt payload rejection, and low-level BSDIFF corruption rejection, create-patch zero-chunk and missing previous/next resource-source failure cleanup, malformed local apply manifest rejection for zero apply chunk size, target offset overflow, source range overflow, and overlapping copy ranges, copy-only patch records with no generated binary payloads, initial legacy filter parser/matcher tests including generated wildcard/ellipsis path and include/exclude section-property matrices, byte-for-byte create-group fixture outputs, CreateFromFilter fixture outputs, byte-for-byte merge fixture outputs, byte-for-byte diff fixture outputs, remove-resource fixture outputs, and 70 process-level Rust resources CLI parity cases mapped to `ResourcesCliTest`/`ResourcesLibraryTest`, including no-command/invalid-command/`--help` top-level legacy help-shape output, operation-specific `--help` and no-argument usage-shape output for all resource commands, dispatcher exit-code/status classes, local export-resources, local and remote-CDN create-bundle manifest/payload bytes, local and remote-CDN unpack-bundle payload bytes, remote-CDN unpack first-run download and second-run cache-hit stats, normal/no-change/chunked/copy-only local create-patch and apply-patch behavior, normal/chunked/old-layout apply-patch payload bytes, and missing previous/next resource input plus missing/corrupt patch payload apply-patch failure cleanup with no output tree; broader bundle corpus, broader patch temp-file cleanup modes, broader filter-file corpus/fuzz coverage, network-backed remote/catalog behavior, cross-host create-group generation, broader detailed CLI output/error text compatibility, and broader apply/unpack corpus coverage are still open. |
| Tier 1 benchmarks | initial/mixed | `cargo run -p xtask -- bench` writes `target/carbon/evidence/bench-tier-local.json`; most rows are Rust-only, and create-group/create-group-from-filter/merge-group/diff-group/remove-resources/create-bundle/create-patch/unpack-bundle/apply-patch have preliminary parity-checked process-to-process legacy/Rust samples with wall time, p50/p95 latency, effective CPU burn, CPU percent, peak RSS, throughput, and 100k-unit linear estimate stats. The evidence now records selected legacy resources CLI paths, CMake build type, `legacy_known_non_debug`, and `optimization_readiness`. |
| Native/parallel/SIMD optimization lane | partial evidence | `scripts/carbon-native-bench.sh bench` now records `release-native`, fat LTO, `target-cpu=native`, and optimized legacy baseline detection in `target/carbon/evidence/bench-tier-local.json`; `scripts/carbon-native-bench.sh io-workloads` records the same native Rust build context for TCP/TLS loopback baseline-vs-bridge rows. Current selected legacy resources binaries are Debug, so optimized-baseline speedup-claim rows remain `0`. Rayon/Tokio, SIMD, bitset, and optimized-index opportunities remain unimplemented and unmeasured. |

## Claims Allowed Now

- The migration has a baseline inventory for `resources`, `scheduler`, `core`, `io`, and `vcpkg-registry`.
- Legacy `resources` is a fresh local CTest gate on this Linux host.
- Legacy `scheduler` now has a local evidence probe and an import path for a supported-platform baseline. On this Linux host, vcpkg bootstrap succeeds but the unsupported `carbon-core:x64-linux` dependency blocks configure; use `legacy-scheduler import` with a non-empty passing Windows x64/macOS CTest artifact, or add an official Linux-supported `carbon-core`/vcpkg triplet, before this can be a green gate.
- Rust `carbon-scheduler-core` can execute the first event-checked semantic parity slice: FIFO run order, targeted `tasklet.run()` ordering for nested and non-nested modes, single-level and multi-level nested tasklet schedule-order fixtures including yield-to-blocked channel wakeups, schedule_remove pause/reinsert, basic blocking send/receive, basic send/receive match, sender-preferred and neutral multi-party channel preference cases, main-side send/receive preference ordering, close cleanup for blocked senders and receivers, receive/send deadlock after children drain, and immediate main receive deadlock.
- Rust `carbon-scheduler-python` has an initial PyO3 smoke surface for `_scheduler`, `_scheduler_debug`, `_scheduler_trinitydev`, and `_scheduler_internal` module population/import from `target/carbon/python`, legacy `scheduler` package import through `PYTHONPATH`, public symbol presence, named `scheduler._C_API` PyCapsule validation with non-null initial ABI-table pointer, callable `PyTasklet_New`, `PyChannel_New`, `PyScheduler_GetCurrent`, and `PyScheduler_GetScheduler` entries, callable tasklet C API type/property entries, callable channel C API type/property entries including preference, callable read-only scheduler counter entries, simple `PyTasklet_Setup` plus `PyScheduler_RunNTasklets` callable execution with run-count updates, initial safe C API tasklet/channel/scheduler smoke paths, an expanded C++ `Scheduler.h` tasklet lifecycle/run-control/channel-preference/invalid-argument/inside-tasklet-send smoke that imports the Rust capsule and calls header-typed tasklet setup/insert/kill/run-count/current/RunNTasklets plus `PyChannel_GetPreference`/`PyChannel_SetPreference` default, neutral/sender, legacy clamping, invalid tasklet/channel/null argument paths, and `PyChannel_Send` from inside a running tasklet, real legacy `capiTest/Tasklet.cpp`, `Channel.cpp`, and `Scheduler.cpp` source-slice runs covering 36 C API tests, an explicit failing in-process probe for those same source slices, a `maturin` wheel build/install smoke proving installed `_scheduler` and installed `scheduler` package import from site-packages, Python-level channel exception/send_throw validation, Python-level Greenlet continuation, callback, thread-cleanup, tasklet throw/kill/traceback, and switch/raise/kill smoke paths, Python-level `scheduler.tasklet(callable)(args)` plus `scheduler.run_n_tasklets(1)` smoke execution, direct `tasklet.run()` for simple queued callables and core-paused bind/remove tasklets, all 210 unchanged legacy scheduler Python unittest cases covering C API exposure, simple tasklet run, args/kwargs, targeted run order, scheduler run order, nested run order, schedule-yield order, multi-level schedule order, schedule/channel callback registration get/set and multiple-thread storage, tasklet kill/invalid construction/weakref/cyclic cleanup/TaskletExit exception smoke, switch-trap raised-callable smoke, channel blocking/deadlock/accounting/preference/refcount/inter-thread/cleanup behavior, block-trap send/receive errors, closed/open channel basics, channel iterator smoke, invalid channel construction, block-trap balance no-mutation checks, all 10 QueueChannel tests, ABI version exposure, `channel` subclass constructor/init shape, preference setting, stable-current `block_trap`, nested-tasklet flag roundtripping, live CoreScheduler handle allocation/mirroring for PyO3 tasklet/channel unbuffered channel state, core-owned live run-queue FIFO/count/remove/pop behavior and scheduled-state authority, explicit core pause/resume lifecycle transitions for covered bind/remove/insert/switch pause paths, core-ID based matched sender/receiver selection plus core-owned immediate peer handoff for covered unbuffered send/receive paths, core snapshot driven sender-preferred pre-receive scheduling probes, core-selected `channel.queue` fronts, and live tasklet runtime snapshots for covered lifecycle paths. This is not full scheduler parity because full in-process legacy C API binary compatibility, final core ownership drain for Python payload handoff/queue identity adapters/Greenlet lifecycle authority, and Carbon IO semantic traces are not yet proven.
- Rust `carbon-resources-core` matches the first resource tools/catalog/Indicies-corpus/filter/create-group/create-from-filter/merge/diff/remove/malformed-imports/local-and-remote-CDN-bundle-create-unpack-bytes/remote-cache-stats/create-bundle-failure-cleanup/missing-chunk-and-source-type-failure-cleanup/patch-manifest-payload-generation-and-local-apply-bytes/create-patch-failure-cleanup/CLI-artifact slice: MD5 string/file checksums, MD5 stream checksums over legacy file chunks, legacy FNV path checksum, gzip decompression/roundtrip with legacy header normalization, chunked gzip stream compression/decompression, FileDataStreamIn chunked reads, FileDataStreamOut byte output, CompressedFileDataStreamOut gzip roundtrip, rolling Adler checksum formula, FindMatchingChunks string cases, FindMatchingChunk file offset cases, CountMatchingChunks patch fixture offsets, ChunkIndex generation/persisted lookup, ChunkIndex checksum-filtered generation/lookup, ResourceTools bundle stream splitting for many files into many uncompressed chunks and one compressed chunk with exact reconstruction, legacy YAML/CSV ResourceGroup import/export including Linux/macOS/Windows create-group YAML, skip-compression YAML, CSV, and prefixed CSV golden round-trips with platform-specific `BinaryOperation` values, large `Indicies` normal and binary-operation v0 CSV to v0.1 YAML conversion plus YAML corpus round-trips, malformed YAML/CSV result categories, BundleGroup and PatchGroup manifest import/export, local BundleGroup create/unpack byte checks, remote-CDN compressed BundleGroup create byte checks, a local create-and-unpack bundle roundtrip with stable YAML and payload byte checks, create-bundle zero-chunk and missing-resource-source failure cleanup, compressed remote-CDN BundleGroup unpack byte checks, 42-chunk local unpack boundary evidence, missing-chunk unpack failure cleanup, remote-requested-local chunk failure cleanup, local-requested-remote compressed chunk failure cleanup, local remote-CDN mirror/cache hit, bad-cache replacement, and checksum-failure checks, remote-CDN CLI first-run download and second-run cache-hit stats, PatchGroup local CDN payload read checks, PatchGroup local BSDIFF payload generation byte checks for normal/chunked/old-layout fixtures, PatchGroup local CDN apply byte checks for normal, chunked, and old-layout fixtures, corrupt local patch payload rejection, low-level BSDIFF corruption rejection, create-patch zero-chunk and missing previous/next resource-source failure cleanup, malformed local apply manifest rejection for zero apply chunk size, target offset overflow, source range overflow, and overlapping copy ranges, apply-patch missing previous/next resource input plus missing/corrupt patch payload failure cleanup, copy-only patch records without binary patch payload generation, selected legacy filter prefix/wildcard/include/exclude matching semantics including generated wildcard/ellipsis path and include/exclude section-property matrices, Linux create-group YAML/CSV/prefix/skip-compression fixture outputs, CreateFromFilter fixture outputs, YAML/CSV merge fixture outputs, CSV diff fixture outputs, YAML remove-resource fixture outputs, and 70 process-level Rust resource CLI parity cases including no-command/invalid-command/`--help` top-level legacy help-shape output, operation-specific `--help` and no-argument usage-shape output for all resource commands, dispatcher exit-code/status classes, local export-resources, local and remote-CDN create-bundle manifest/payload bytes, create-bundle zero-chunk and missing-resource-source failure cleanup, local and remote-CDN unpack-bundle payload bytes, missing-chunk and source-type mismatch unpack failure cleanup, normal/no-change/chunked/copy-only/zero-chunk/missing-source local create-patch behavior, and normal/chunked/old-layout/copy-only/missing-previous/missing-next/missing-payload/corrupt-payload apply-patch payload/failure behavior.
- Local benchmark samples exist for the current scheduler fixture runner, resource MD5, resource gzip, resource filter matching, create-group, create-group-from-filter, merge-group, diff-group, remove-resources, create-bundle, create-patch, unpack-bundle, and apply-patch workloads. The create-group, create-group-from-filter, merge-group, diff-group, remove-resources, create-bundle, create-patch, unpack-bundle, and apply-patch rows are parity checked and compare process-to-process execution with throughput, wall time, p50/p95 latency, effective CPU burn, CPU percent, peak RSS, and 100k-unit linear estimates. Current comparable resource rows use selected legacy `resources_debug` binaries against Rust `release-native`; `optimization_readiness.speedup_claim_eligible_comparisons=0`, so they support narrow preliminary local catalog, bundle, and patch workflow observations only, not optimized-baseline speedup claims.
- Local IO workload samples exist for loopback TCP and TLS request cycles. These compare a Python stdlib baseline to the Rust scheduler Python bridge with five process samples per implementation by default, aggregate 400 TCP and 120 TLS request latency samples, and record throughput, effective CPU burn, CPU percent, peak RSS, and 100k-request linear estimates. The same gate validates three timing-free `fixtures/io` normalized semantic trace fixtures for socket recv/send wake, SSL read/write wake, and SSL send_throw error wake, proves the Rust scheduler capsule supports IO-facing `PyChannel_GetBalance`, `PyChannel_GetQueue`, `PyChannel_Send`, and `PyChannel_SendThrow` blocked-receiver semantics through a compiled `Scheduler.h` C++ consumer, and records the legacy Carbon IO trace comparison as blocked on supported legacy artifacts. They are realistic local workload stats and initial fixture/C API semantic evidence, not legacy Carbon IO speedup claims.
- A native benchmark lane now exists through the `release-native` Cargo profile and `scripts/carbon-native-bench.sh`; the latest benchmark and IO workload JSON both record `target_cpu_native=true`, and benchmark evidence records selected legacy baseline detection. Native/LTO build context can be cited for the current narrow resource comparison rows and non-comparable IO baseline-vs-bridge observations, but speedup language remains blocked until selected legacy resources baselines are known non-debug.
- `xtask report-readiness` reports which evidence gates are still missing before the HTML report can be generated.
- `xtask report-progress` writes a non-final HTML progress artifact to `target/carbon/report/progress.html`.
- `xtask report` has a gated HTML renderer and refuses to write the final report until all required evidence files pass.

## Claims Not Allowed Yet

- No broad Rust speedup or throughput gain can be claimed.
- No scheduler parity claim can be made beyond the 39 passing semantic fixtures and the partial Rust Python bridge subset.
- No claim can be made that the Rust crates fully own scheduler tasklet/channel lifecycle yet; `carbon-scheduler-core` has Rust-owned channel handles, run queues, tasklet runtime snapshots, and explicit pause/resume transitions for covered bind/remove/insert/switch pause paths, and the live PyO3 bridge now mirrors unbuffered channel balance/preference/block-trap/blocked-queue transitions, uses core operation results for matched tasklet selection in covered unbuffered paths, uses core queue-front results for `channel.queue`, and checks core paused snapshots for direct-run paused eligibility, but `carbon-scheduler-python` still owns Python payload handoff, authoritative Greenlet tasklet lifecycle, callbacks, refcount/GC, pending payload close-state details, and broader PyObject queue identity adapters.
- No full C API compatibility claim can be made for Rust; the current `_C_API` and unchanged-test coverage proves a named capsule shell, a non-null ABI-table pointer, type/exception slots, callable constructors/current/scheduler getters, callable tasklet/channel type/property entries including channel preference get/set/clamping, callable read-only scheduler counter entries, a simple setup/run callable workload, initial safe C API tasklet/channel/scheduler smoke paths, an expanded C++ `Scheduler.h` tasklet lifecycle/run-control/channel-preference/invalid-argument/inside-tasklet-send smoke, real legacy `capiTest/Tasklet.cpp`, `Channel.cpp`, and `Scheduler.cpp` source-slice runs covering 36 C API tests in child-process mode, installed release-wheel import/package smoke, an IO-facing C++ `Scheduler.h` channel balance/send/send_throw semantic smoke, callback registration/invocation smoke paths, active Greenlet/channel exception/send_throw/thread-cleanup/switch/raise/kill smoke paths, and the full 210-test legacy Python scheduler suite. The new in-process source-slice probe is failing at repeated interpreter reinitialization, so full in-process legacy `SchedulerCapiTest`/`capiTest` binary coverage, broader packaging matrix, broader `c_channel` corpus coverage, and broader failure-mode hardening remain blockers for a full compatibility claim.
- No legacy Carbon `io` socket/SSL workload parity claim can be made.
- No resources byte parity claim can be made for Rust beyond the initial resource tools/catalog/filter/create-group/create-from-filter/merge/diff/remove/malformed-imports/local-and-remote-CDN-bundle-create-unpack/remote-cache-stats/bundle-and-patch-failure-cleanup/patch-manifest-payload-generation/local-apply/CLI-artifact tests.
- No broad performance speedup claim can be made; only the preliminary local process-level create-group, create-group-from-filter, merge-group, diff-group, remove-resources, create-bundle, create-patch, unpack-bundle, and apply-patch rows are legacy/Rust comparable today, and those rows use selected legacy debug binaries. TCP/TLS loopback rows are baseline-vs-bridge workload stats only.
- No 10x, SIMD, Rayon/Tokio, or low-level data-structure optimization claim can be made yet; the current native-build evidence only proves the narrow benchmark rows and IO loopback bridge rows ran under `release-native` with `target-cpu=native`, while `optimization_readiness` proves the selected legacy resources baseline is still Debug.
- No cross-network/container performance claim can be made.

## Required Before HTML Report

| Requirement | Status |
| --- | --- |
| Every claimed scheduler feature has a passing legacy and Rust fixture/test. | partial |
| Every claimed resources feature has a passing legacy and Rust fixture/test. | partial |
| Every final performance table row has repeated local benchmark samples for legacy and Rust, with resource stats where comparable speed is unavailable. | partial |
| Every benchmark row records parity status; failed parity rows are marked `not comparable`. | partial |
| Architecture improvements are marked `measured` or `expected`. | partial |
| PyO3 bridge ownership is drained so `carbon-scheduler-core`/FFI handles own tasklet/channel/scheduler lifecycle state. | partial: `CoreScheduler` channel handle API, run-queue state, tasklet runtime snapshots, and explicit pause/resume lifecycle transitions exist; live PyO3 tasklet/channel objects mirror unbuffered channel balance/preference/block-trap/blocked-queue transitions, covered send/receive transfer selection now uses core sender/receiver IDs plus balance and core-owned immediate peer handoff, sender-preferred pre-receive scheduling probes use core snapshots, `channel.queue` uses core queue-front results, and covered bind/remove/insert/switch pause paths use core pause/resume; Python payload handoff, authoritative Greenlet lifecycle, queue identity adapters, callbacks, and broader scheduling ownership remain open |
| Unresolved gaps are linked to `reviews/tasks.md`. | partial |

## Recommended Report Shape Once Unblocked

Use the sections from the migration plan, but keep the executive summary evidence-first:

1. Current evidence gates.
2. Remaining blockers.
3. Scheduler parity and benchmark results.
4. Resources parity and benchmark results.
5. Realistic IO workload results.
6. Architecture improvements with measured/expected labels. This is currently a documented reporting requirement, not a separate JSON evidence gate.
7. Next task list linked to `reviews/tasks.md`.
