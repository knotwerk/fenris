# Coverage Map

This map defines the tests needed before parity can be claimed. Existing legacy tests are source of truth; many Rust tests and realistic workloads are now implemented, but the remaining gaps below still block full parity claims.

## Scheduler Coverage

| Feature area | Legacy tests/source | Existing fixture | Rust test status | Realistic workload status | Gaps |
| --- | --- | --- | --- | --- | --- |
| FIFO scheduler run order | `test_scheduler.py::test_scheduler_run_order` | `fixtures/scheduler/run_order.json` | covered | `cargo run -p xtask -- scheduler-fixtures` | Python object/API parity still belongs to FFI tasks. |
| Targeted `tasklet.run()` | `test_tasklet_run_order*` | `tasklet_run_nested_target_first`, `tasklet_run_nested_target_middle`, `tasklet_run_no_nested_target_middle` | covered | `cargo run -p xtask -- scheduler-fixtures` | Nested and non-nested fixture slice passes; Python object/API parity still belongs to FFI tasks. |
| Nested tasklets | `test_nested_tasklet_run_order*` | `nested_tasklet_run_order`, `nested_tasklet_run_order_no_nested`, `nested_tasklet_schedule_order`, `nested_tasklet_schedule_order_no_nested`, `multi_level_nested_schedule_order`, `multi_level_nested_schedule_order_no_nested`, `nested_yield_to_blocked`, `nested_yield_to_blocked_no_nested` | covered | `cargo run -p xtask -- scheduler-fixtures` | Python object/API parity still belongs to FFI tasks. |
| `scheduler.schedule` / `schedule_remove` | `test_scheduler.py::TestSchedule*` | `nested_tasklet_schedule_order`, `nested_tasklet_schedule_order_no_nested`, `schedule_remove_reinsert_paused_tasklet` | partial | `cargo run -p xtask -- scheduler-fixtures` | Schedule-order and schedule-remove pause/reinsert state are covered; switch-trap schedule/schedule_remove errors and broader reschedule-position cases remain open. |
| Bounded runs and timeout counters | `test_utils.py::TestWithLimit`, C API timeout tests | `scheduler_run_n_tasklets_one_fifo_boundary`, `scheduler_timeout_zero_runs_one_tasklet`, `scheduler_timeout_large_counts_all_tasklets`, `scheduler_timeout_zero_leaves_remaining_scheduled` | partial | `cargo run -p xtask -- scheduler-fixtures`; `cargo run -p xtask -- rust-scheduler-python` | Real legacy `Scheduler.cpp` source-slice tests now cover C API `RunNTasklets`, practical `RunWithTimeout`, and completed/switched timeout counters through the Python bridge; pure core fixtures now cover one-tasklet bounded pumping, expired-timeout progress, simple completed/switched timeout counters, and the remaining scheduled tasklets after an expired timeout. Broader nested-timeout and cleanup parity remain open. |
| Tasklet lifecycle | `test_tasklet.py::test_run*`, `test_remove*`, `test_kill_tasklet`, `test_paused` | `tasklet_bind_setup_rebind_run`, `tasklet_lifecycle_remove_insert_switch_times`, `tasklet_kill_pending_and_immediate`, `tasklet_unbind_paused`; `CoreTaskletSnapshot` bridge tests | partial | `cargo run -p xtask -- scheduler-fixtures`; `cargo run -p xtask -- rust-scheduler-python` | Core fixtures cover bind/setup/rebind, remove/insert/switch counts, kill pending/immediate, unbind of paused tasklets, and explicit core pause/resume transitions. The handle API exposes `CoreTaskletSnapshot`; the PyO3 bridge mirrors live tasklet alive/scheduled/paused/times_switched_to for covered setup/run/finish/block/continuation/clear/kill paths, uses core pause/resume for bind/remove/insert/switch pause paths, and checks core paused snapshots for direct-run eligibility. Broader Greenlet continuation and Python object lifetime semantics remain in the FFI gate. |
| Direct switch and switch trap | `test_scheduler.py::TestSwitch*`, `TestSwitchTrap*` | `tasklet_lifecycle_remove_insert_switch_times` | partial | `cargo run -p xtask -- scheduler-fixtures`; `cargo run -p xtask -- rust-scheduler-python` | Direct switch to a queued tasklet is covered; switch-trap error paths and post-error state remain Python bridge/FFI work. |
| Blocking send/receive | `test_channel.py::test_blocking_send`, `test_blocking_receive` | `blocking_send.json`, `blocking_receive.json`; `CoreScheduler` handle tests | partial | `cargo run -p xtask -- scheduler-fixtures`; `cargo run -p xtask -- rust-scheduler-python` | Core fixtures and Python bridge tests cover blocking balance/deadlock accounting, Greenlet channel yields/resume, block-trap send/receive errors, closed/open channel basics, and selected handoff order. The `CoreScheduler` API covers Rust-owned tasklet/channel handles, balance signs, blocked queue fronts, block-trap no-mutation, close/open/clear, and remove-blocked cleanup; the PyO3 bridge now allocates live core handles and mirrors unbuffered channel balance/preference/block-trap/blocked-queue transitions through those handles. Python payload transfer, queue identity adapters, and broader queue wake ownership remain open. |
| Channel transfer | `test_non_blocking_send`, `test_non_blocking_receive`, reschedule tests | `send_receive_match.json`, `channel_preference_sender`, `channel_preference_neither_simple`, `channel_preference_neither_tasklet_transfer`, `channel_preference_neither_multi_party_order`, `channel_main_side_preference_matrix`, `channel_queue_introspection_order` | partial | `cargo run -p xtask -- scheduler-fixtures` | Receiver-preferred transfer, sender-preferred transfer, simple neutral preference, tasklet neutral transfer, multi-party neutral ordering, main-side send/receive preference order, and blocked queue snapshots are covered; Python identity and broader bridge behavior remain open. |
| Deadlock | `test_main_tasklet_*deadlock*` | `deadlock.json`, `main_receive_immediate_deadlock`, `main_send_immediate_deadlock`, `main_send_deadlock_after_running_child_tasklets` | partial | `cargo run -p xtask -- scheduler-fixtures` | Immediate receive/send deadlock and receive/send after runnable child drain are covered; broader main-with-children variants remain open. |
| Channel preference | `test_preference_sender`, `test_preference_receiver`, `test_preference_neither*` | `send_receive_match`, `channel_preference_sender`, `channel_preference_sender_receiver_first`, `channel_preference_neither_simple`, `channel_preference_neither_tasklet_transfer`, `channel_preference_neither_multi_party_order`, `channel_main_side_preference_matrix` | partial | `cargo run -p xtask -- scheduler-fixtures` | Preference `-1`, sender preference `1`, simple neutral preference `0`, receiver-first sender preference, neutral tasklet transfer, multi-sender/multi-receiver neutral ordering, and main-side preference order are covered; Python-visible tasklet identity assertions remain bridge work. |
| Channel close/open/iterator | `test_closing`, `test_open`, `test_iterator_on_closed`, send/receive closed | `channel_close_open_parity`, `channel_kill_error_cleanup_while_blocked`, `channel_close_cleanup_blocked_receivers` | partial | `cargo run -p xtask -- scheduler-fixtures`; `cargo run -p xtask -- rust-scheduler-python` | Core close/open state, closed send/receive errors, and close cleanup for blocked senders/receivers are covered; Python iterator protocol and broader FFI exception text remain bridge work. |
| Channel exception transfer | `test_send_exception`, `test_send_throw*` | `channel_exception_payloads` | partial | `cargo run -p xtask -- scheduler-fixtures`; `cargo run -p xtask -- rust-scheduler-python` | Core symbolic exception payload transfer is covered; Python exception object/value/traceback fidelity and unchanged-suite promotion remain in the bridge gate. |
| QueueChannel | `test_queuechannel.py::*` | Python bridge wrapper, not pure-core fixture | covered for current bridge path | `cargo run -p xtask -- rust-scheduler-python` | The real legacy `scheduler.QueueChannel` wrapper subclasses the Rust-backed base channel, and all 10 unchanged legacy QueueChannel tests pass, covering buffered sends, balance/length, queued data, blocking receive wakeup, queued exception/throw, main-tasklet empty receive error, nested blocked receiver order, block-trap receive rejection, and main receive drain. Pure-core buffered-channel fixtures are deferred unless QueueChannel is intentionally moved out of Python. |
| Fixture teardown invariants | `test_utils.py::SchedulerTestCaseBase.tearDown` | all scheduler fixtures now run trace-gate structural invariant checks | partial | `cargo run -p xtask -- scheduler-fixtures` | The fixture gate validates event sequence order, cached/calculated run count equality, active/all-time tasklet counts, blocked tasklet state, channel balance, queue-front consistency, and channel/tasklet blocked-queue cross-links. Python teardown refcount, active channel/manager cleanup, and nested-mode reset coverage remain open. |
| PyO3 Python boundary smoke | `test_utils.py` import shape and `scheduler/__init__.py` re-export shape | none | partial | `cargo run -p xtask -- rust-scheduler-python`; `cargo run -p xtask -- io-workloads` | Initial `_scheduler` module population, normal CPython import of `target/carbon/python/_scheduler*.so` for release/debug/trinitydev/internal, legacy `scheduler` import through `PYTHONPATH`, public symbol presence, named `_C_API` capsule shell validation with callable constructors/current/scheduler getters, tasklet/channel type/property entries, read-only scheduler counter entries, simple setup/run callable execution, initial safe C API tasklet/channel/scheduler smoke paths, expanded C++ `Scheduler.h` tasklet lifecycle/run-control/channel-preference/invalid-argument/inside-tasklet-send smoke, generated source-slice binaries compiling/running the real legacy `capiTest/Tasklet.cpp`, `Channel.cpp`, and `Scheduler.cpp` tests one test per child process, local `maturin` release-wheel build/install smoke from a fresh virtualenv, IO-facing `Scheduler.h` channel balance/send/send_throw semantic smoke, timing-free `fixtures/io` normalized semantic trace fixtures for socket recv/send wake, SSL read/write wake, and SSL send_throw error wake, Python-level Greenlet continuation, channel exception/send_throw, callback, thread cleanup, tasklet throw/kill/traceback, and switch/raise/kill smoke paths, direct `tasklet.run()` for simple queued callables and core-paused bind/remove tasklets, all 210 unchanged legacy scheduler Python unittest cases, callback registration get/set storage, tasklet kill/invalid construction/weakref/cyclic cleanup/TaskletExit smoke, switch-trap raised-callable smoke, channel blocking/deadlock/accounting/preference/refcount/inter-thread/cleanup behavior, block-trap send/receive errors, closed/open channel basics, invalid channel construction, channel iterator smoke, block-trap balance no-mutation checks, ABI version exposure, `channel` subclass constructor/init shape, all 10 QueueChannel legacy tests, preference setting, stable-current `block_trap`, nested-tasklet flag, live CoreScheduler handle allocation/mirroring for PyO3 tasklet/channel unbuffered channel state, and live tasklet runtime snapshots plus explicit core pause/resume lifecycle transitions for covered bind/remove/insert/switch paths are covered. The evidence records `core_ownership_status=partial`: the PyO3 crate is a compatibility bridge, `carbon-scheduler-core` owns an initial CoreScheduler tasklet/channel handle API, and live Python tasklet/channel objects now carry those handles for mirrored balance/preference/block-trap/blocked-queue transitions plus mirrored tasklet alive/scheduled/paused/times_switched_to. Python payload transfer, authoritative Greenlet lifecycle scheduling, callbacks, refcount/GC, and exact PyObject queue identity are still bridge-owned. The optional in-process source-slice probe now runs and currently fails on the second embedded import after `Py_FinalizeEx()` because `_scheduler.channel` is `None`; broader wheel flavor/dependency/install matrices, full in-process legacy C API binary coverage, final core ownership drain, captured legacy Carbon IO traces, and broader failure-mode hardening remain open. |
| Build-flavor imports | `test_utils.py` module import block | none | partial | `cargo run -p xtask -- rust-scheduler-python` | Release/debug/trinitydev/internal module imports and `sys.modules['_scheduler']` aliasing work from `target/carbon/python`; the release-flavor unchanged legacy unittest suite runs from that package path; an installed release wheel imports `_scheduler` and the legacy `scheduler` package from site-packages; broader flavor-matrix wheel execution remains open. |
| Scheduler callbacks | callback tests in `test_scheduler.py`, `test_channel.py` | none | partial | `cargo run -p xtask -- rust-scheduler-python` | Python-level set/get storage is covered for schedule and channel callbacks; trace callback points, Python callable invocation, fast C callbacks, and lifetime/refcount cleanup remain open. |
| C API capsule | `tests/capiTest/*.cpp`, `_C_API` exposure test | none | partial | `cargo run -p xtask -- rust-scheduler-python`; `cargo run -p xtask -- io-workloads` | Named `scheduler._C_API` PyCapsule validation, non-null initial ABI-table pointer, type/exception slots, callable constructors/current/scheduler getters, tasklet/channel type/property entries, callable read-only scheduler counter entries, a simple `PyTasklet_Setup` plus `PyScheduler_RunNTasklets` callable workload, safe C API tasklet/channel/scheduler smoke paths, channel preference get/set/clamping, invalid tasklet/channel/null argument rejection, C API `PyChannel_Send` from inside a running tasklet with blocked sender balance/queue accounting, unchanged `TestCAPIExposure.test_has_capi_attribute`, an expanded C++ `Scheduler.h` tasklet lifecycle/run-control/channel-preference smoke, real `capiTest/Tasklet.cpp`, `Channel.cpp`, and `Scheduler.cpp` source-slice runs covering 36 C API tests, and an IO-facing C++ `Scheduler.h` smoke covering `PyChannel_GetBalance`, `PyChannel_GetQueue`, `PyChannel_Send`, and `PyChannel_SendThrow` blocked-receiver semantics are covered. The same source slices now have an in-process probe, currently failing at repeated interpreter reinitialization/finalization; full in-process legacy `SchedulerCapiTest`/`capiTest` binary coverage, broader `c_channel` corpus coverage, and broader ABI failure tests remain open. |
| Thread/cross-thread behavior | thread tests in `test_channel.py`, `test_tasklet.py`, callbacks | none | deferred | `cargo run -p xtask -- io-workloads` has loopback socket/SSL workload stats and fixture-only semantic trace shape evidence | Need owner-thread bridge, captured legacy Carbon IO traces, and Loom/interleaving tests. |

## Resources Coverage

| Feature area | Legacy tests/source | Rust test status | Gaps |
| --- | --- | --- | --- |
| YAML resource groups | `ResourcesLibraryTest.cpp`, `ResourceGroupImpl.cpp` | partial | Small ResourceGroup YAML fixtures and the large `Indicies/ResourceGroup_v0_1_0.yaml` plus binary-operation `Indicies/BinaryResourceGroup_v0_1_0.yaml` corpus round-trip byte-for-byte; broader YAML corpus and CLI output/error text remain open. |
| CSV resource groups | CLI/library fixtures under `tests/testData/CreateResourceFiles` and `tests/testData/Indicies` | partial | Small CSV fixtures, v0 prefix paths, and large `Indicies` v0 CSV to v0.1 YAML conversion for normal and binary-operation catalogs are covered; broader platform-specific mode/path goldens remain open. |
| Bundle create/unpack | `BundleResourceGroupImpl.cpp`, `ResourceToolsTest.ResourceChunking*`, CLI tests | partial | BundleGroup manifest import/export, ResourceTools many-files-to-many-uncompressed-chunks and many-files-to-single-compressed-chunk reconstruction, local bundle create manifest/chunk bytes, remote-CDN compressed bundle create bytes, local unpack bytes, compressed remote-CDN unpack bytes, create-bundle zero-chunk and missing-resource-source failure cleanup, 42-chunk boundary unpack evidence, missing-chunk unpack failure cleanup, remote-requested-local chunk failure cleanup, local-requested-remote compressed chunk failure cleanup, local remote-CDN mirror/cache hit and bad-cache replacement behavior, process-level remote-CDN first-run download and second-run cache-hit stats, and create/unpack bundle CLI payload/manifest output are covered for current fixtures; broader corpus, network-backed remote/catalog behavior, and error behavior remain open. |
| Patch create/apply | `PatchResourceGroupImpl.cpp`, patch fixtures | partial | PatchGroup manifests round-trip against create-patch, chunked create-patch, and old patch goldens; local CDN patch payload bytes validate against manifest location, uncompressed size, and checksum; corrupt local patch payloads are rejected before output; low-level BSDIFF corruption rejects short payloads, invalid headers, truncated streams, and target-length mismatches; Rust-generated BSDIFF payloads match normal, chunked, and old-layout local CDN payload bytes; normal, no-change, and chunked local create-patch CLI output matches expected legacy semantics/goldens; zero-chunk and missing previous/next resource-source create-patch failure cleanup is covered; a synthetic copy-only create/apply CLI case emits no generated binary patch payload records and reconstructs the latest resource; normal, chunked, and old-layout local patch apply fixtures reconstruct expected resource bytes, including old-layout no-removal semantics; malformed local apply manifests reject zero apply chunk size, target offset overflow, source range overflow, and overlapping copy ranges; normal, chunked, and old-layout apply-patch CLI output reconstructs the expected payload directories; missing previous/next resource inputs and missing/corrupt patch payload apply failures leave no output tree. Broader temp-file cleanup modes and larger corpus parity remain open. |
| Compression | `GzipCompressionStream.cpp`, `CompressedFileDataStreamOut.cpp`, compressed file fixtures | partial | Gzip legacy fixture decompression, roundtrip, exact legacy header normalization, chunked gzip stream compression/decompression, CompressedFileDataStreamOut gzip roundtrip, and remote-CDN bundle payload byte parity are covered; broader compressed-file corpus and streaming edge cases remain open. |
| Checksums, matching chunks, and chunk indexes | `Md5ChecksumStream.cpp`, `ResourceTools.cpp`, `ChunkIndex.cpp` | partial | MD5 string/file checksums, MD5 stream checksums over legacy file chunks, legacy FNV path checksum, rolling checksum formula, FindMatchingChunks string cases, FindMatchingChunk file offset cases, CountMatchingChunks patch fixture offsets, ChunkIndex generation/persisted lookup, and ChunkIndex checksum-filtered generation/lookup are covered; larger corpus checks remain open. |
| Filtering | `ResourceFilter.cpp`, `ResourceToolsFilterTest.cpp` | partial | Prefix maps, wildcard/include/exclude/local respath quirks, multi-prefix cases, a generated wildcard/ellipsis path matrix, generated include/exclude section-property cases, filter index mapping parsing, and CreateFromFilter fixture outputs are covered; broader filter-file corpus and fuzz tests remain open. |
| Remote IO | `Downloader.cpp` | open | Need local object-store/probe tests before S3-like support. |
| CLI behavior | `ResourcesCliTest.cpp` | partial | `cargo run -p xtask -- rust-resources` | Seventy process-level Rust resources CLI parity cases now prove no-command/invalid-command/`--help` top-level legacy help-shape output, operation-specific `--help` and no-argument usage-shape output for all resource commands, dispatcher exit-code/status classes, create-group YAML/CSV/prefix/skip-compression, create-group-from-filter, local export-resources for directory and filter creation, local and remote-CDN create-bundle manifest/payload output, create-bundle zero-chunk and missing-resource-source failure cleanup, local and remote-CDN unpack-bundle payload output including 42-chunk boundary evidence, missing-chunk failure cleanup, remote-requested-local chunk failure cleanup, local-requested-remote compressed chunk failure cleanup, and remote-CDN mirror download/cache-hit stats, normal/no-change/chunked/copy-only local create-patch and apply-patch output, create-patch zero-chunk and missing previous/next resource-source failure cleanup, normal, chunked, and old-layout apply-patch payload output, apply-patch missing previous/next resource input plus missing/corrupt patch payload failure cleanup with no output tree, merge YAML/CSV, diff additions/changes/subtractions, remove-resources, ignore-missing success, invalid remove-list failure, and unknown-resource failure semantics against legacy goldens; broader detailed CLI output/error text compatibility, broader apply/unpack corpus coverage, and broader failure behavior remain open. |

## Current Legacy Harness Status

| Component | Local status | Notes |
| --- | --- | --- |
| `resources` | green | `121/121` CTest tests passed on Linux probe per `test-harness-status.md`. |
| `scheduler` | partial green | Python discovery finds `210` tests, all 210 unchanged legacy scheduler Python unittest cases now pass against the Rust extension, an expanded C++ `Scheduler.h` tasklet lifecycle/run-control/channel-preference/invalid-argument/inside-tasklet-send smoke passes, real legacy `capiTest/*.cpp` source slices pass in child-process mode, the new in-process source-slice probe records the current repeated-interpreter failure, and an installed release-wheel smoke passes through `cargo run -p xtask -- rust-scheduler-python`. The local legacy scheduler Linux build is blocked by unsupported `carbon-core:x64-linux`, but `legacy-scheduler import` can now validate a supported Windows/macOS CTest artifact when one is available. Full in-process legacy C API binary tests, broader packaging matrix, and Carbon IO semantic traces remain open before full scheduler parity can be claimed. |
| `io` | partial green | `cargo run -p xtask -- io-workloads` records TCP/TLS loopback request-cycle stats with five process samples per implementation by default, aggregates request latency samples, validates the timing-free `fixtures/io` normalized semantic trace corpus, and runs the IO-facing `Scheduler.h` channel wake/send_throw semantic smoke. Full legacy `carbonio`/`_socket`/`_ssl` extension semantic trace comparison remains open. |

## Review Status

Wave 1 scheduler core review is complete and consolidated in `reviews/findings.jsonl`. Later waves remain queued or partial; they should not be treated as parity evidence until their pair notes move to `done`.
